executor: slurm
jobs: 1
use-conda: true
#slurm-extra-args: "--exclude=pauli1"
#max-failures: 20

# Optional but common:
default-resources:
  #slurm_partition: "compute"
  #slurm_account: "myacct"
  runtime: 5         # minutes
  mem_mb_per_cpu: 1000 # MB per CPU

# threads = --cpus-per-task on SLURM