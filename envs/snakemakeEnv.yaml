name: snakemake_env
channels:
  - conda-forge
  - defaults
dependencies: # not tested yet. Just an example
  - python=3.12
  - numpy=1.26.4
  - matplotlib=3.10.6
  - pandas=2.1.4
  - networkx=3.4
  - snakemake=8.10 # optional: if you run inside this environment

# part of the standard library: os, sys, math, pickle